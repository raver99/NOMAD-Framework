## Backlog

Work and ideas live in `BACKLOG.md` at the project root. Its header states the line format and what
each section means; follow that. An item's status is which section its line sits in and is recorded
nowhere else, so changing state means moving the line. IDs are assigned once and never reused.
Detail files under `backlog/` hold context and reasoning, never metadata.
